local player = game.Players.LocalPlayer
local runService = game:GetService("RunService")

-- Create UI
local screenGui = Instance.new("ScreenGui", game.CoreGui)
local frame = Instance.new("Frame", screenGui)
frame.Size = UDim2.new(0, 250, 0, 100)
frame.Position = UDim2.new(0.5, -125, 0.2, 0)
frame.BackgroundColor3 = Color3.fromRGB(10, 10, 20)
frame.BorderSizePixel = 2
frame.Active = true
frame.Draggable = true -- Allow dragging

local uiGlow = Instance.new("UIStroke", frame)
uiGlow.Color = Color3.fromRGB(255, 0, 0)
uiGlow.Thickness = 3

local button = Instance.new("TextButton", frame)
button.Size = UDim2.new(1, 0, 0.5, 0)
button.Text = "ENABLE AUTO FARM"
button.BackgroundColor3 = Color3.fromRGB(50, 0, 0)
button.TextColor3 = Color3.fromRGB(255, 50, 50)
button.Font = Enum.Font.SourceSansBold
button.TextSize = 18

local statusLabel = Instance.new("TextLabel", frame)
statusLabel.Size = UDim2.new(1, 0, 0.5, 0)
statusLabel.Position = UDim2.new(0, 0, 0.5, 0)
statusLabel.Text = "Status: Idle"
statusLabel.BackgroundTransparency = 1
statusLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
statusLabel.Font = Enum.Font.SourceSansBold
statusLabel.TextSize = 16

-- Variables
local farming = false

-- Anti-AFK Script
local vu = game:GetService("VirtualUser")
game:GetService("Players").LocalPlayer.Idled:Connect(function()
    vu:CaptureController()
    vu:ClickButton2(Vector2.new())
end)

-- Function to activate ClickDetectors
local function activateClickDetectors()
    for _, obj in pairs(workspace:GetDescendants()) do
        if obj:IsA("ClickDetector") and obj.Parent and obj.Parent.Parent == workspace.BadTimeTrioBoss then
            fireclickdetector(obj)
            print("Activated ClickDetector:", obj.Parent.Name)
        end
    end
end

-- Function to kill BadTimeTrioBoss
local function killBadTimeTrioBoss()
    local boss = workspace:FindFirstChild("BadTimeTrioBoss")
    if boss then
        local head = boss:FindFirstChild("Head")
        if head then
            head:Destroy()
            print("Killed BadTimeTrioBoss by removing the head!")
        else
            print("Head not found on BadTimeTrioBoss.")
        end
    else
        print("BadTimeTrioBoss not found, waiting for respawn...")
    end
end

-- Function to auto-farm
local function autoFarm()
    while farming do
        statusLabel.Text = "Status: Farming..."
        activateClickDetectors()
        killBadTimeTrioBoss()
        task.wait(1) -- Adjust delay if needed
    end
    statusLabel.Text = "Status: Idle"
end

-- Toggle Button
button.MouseButton1Click:Connect(function()
    farming = not farming
    if farming then
        button.Text = "DISABLE AUTO FARM"
        button.BackgroundColor3 = Color3.fromRGB(100, 0, 0)
        task.spawn(autoFarm) -- Start farming in a separate thread
    else
        button.Text = "ENABLE AUTO FARM"
        button.BackgroundColor3 = Color3.fromRGB(50, 0, 0)
    end
end)
